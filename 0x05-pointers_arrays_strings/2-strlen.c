#include "main.h"
#include <stdio.h>

/**
*_strlen - returns the length of a string..
*@s: value for length.
*Return: not.
*/

int _strlen(char *s)

{
	int len;

	len = strlen(*s);


	return (len);


}
